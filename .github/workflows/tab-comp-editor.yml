name: Tab comp editor

on:
  push:
    branches:
      - lot-logic-public-site

permissions:
  contents: write

jobs:
  apply-and-build:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: lot-logic-public-site
          fetch-depth: 0
      - run: python .github/scripts/tab_comp_editor.py
      - run: python .github/scripts/update_comp_guard.py
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add components/evaluation/evaluation-workspace.tsx scripts/ensure-expand-search-source.mjs
          if git diff --cached --quiet; then
            echo "No comp editor changes to commit."
            exit 0
          fi
          git commit -m "Add tabbed comp editor"
          git push origin HEAD:lot-logic-public-site
