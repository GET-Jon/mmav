name: Refine evaluator recommendation

on:
  push:
    branches:
      - lot-logic-public-site

permissions:
  contents: write

jobs:
  refine-and-build:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: lot-logic-public-site
          fetch-depth: 0

      - name: Apply evaluator recommendation refinement
        run: python .github/scripts/refine_evaluator_recommendation.py

      - name: Fix evaluator derived-data ordering
        run: python .github/scripts/fix_refine_evaluator_order.py

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Commit validated changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add components/evaluation/evaluation-workspace.tsx lib/dealer-fit/calculate-dealer-fit.ts lib/dealer-fit/rules.ts
          if git diff --cached --quiet; then
            echo "No evaluator changes to commit."
            exit 0
          fi
          git commit -m "Refine evaluator recommendation hierarchy"
          git push origin HEAD:lot-logic-public-site
